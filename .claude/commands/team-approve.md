An agent wants to join your team. Request details: $ARGUMENTS

Review the request and decide whether to approve or deny it.

Use the `approve_join` tool to accept, or `deny_join` tool to reject.
Both require the team name and the pending agent's agent_id.

Your agent_token is available in the AGENT_TOKEN environment variable (used automatically by the tools).
